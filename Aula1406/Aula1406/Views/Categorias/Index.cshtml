@model IEnumerable<Aula1406.Models.Categoria>

@{
    ViewBag.Title = "Index";
}

<h2>@ViewBag.Title</h2>

@foreach(var item in Model)
{
    @Html.LabelFor(m => item.Nome)
    @Html.DisplayFor(m => item.Nome)
}

